#[macro_use] extern crate rocket;


#[rocket::main]
async fn main() {
    /* 
    let rocket = rocket::build()
        .mount("/", account::gets::routes()).manage(oauth2_client)
            .ignite().await?
            .launch().await?;
    */
            
}